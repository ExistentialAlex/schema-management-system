---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: schema-manager-server
spec:
  template:
    metadata:
      annotations:
    spec:
      containers:
        - name: schema-manager-server
          image: europe-west1-docker.pkg.dev/uk-cti-adtech-platform-prod/schema-manager/schema-manager-server:${GIT_TAG}
