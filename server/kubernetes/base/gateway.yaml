---
apiVersion: launchpad.sky.uk/v1alpha1
kind: Gateway
metadata:
  name: schema-manager-server
spec:
  realms:
    - name: ping
      auth: none
      allowInternet: false
      allowOnPrem: true
      routes:
        - path: / # https://schema-manager-server.internal.test.adtechplatform.sky.com/
          methods:
            - GET
            - POST
            - PUT
            - PATCH
            - DELETE
            - OPTIONS

  services:
    - name: schema-manager-server
      port: 3000
