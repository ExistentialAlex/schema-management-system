---
apiVersion: launchpad.sky.uk/v1alpha1
kind: Gateway
metadata:
  name: schema-manager-app
spec:
  realms:
    - name: ping
      auth: none
      allowInternet: false
      allowOnPrem: true
      routes:
        - path: / # https://schema-manager-app.internal.test.adtechplatform.sky.com/
          methods:
            - GET

  services:
    - name: schema-manager-app
      port: 80
