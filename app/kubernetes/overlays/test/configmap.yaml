apiVersion: v1
kind: ConfigMap
metadata:
  name: schema-manager-configmap
data:
  config.json: |
    {
      "VITE_API_BASE_URL": "https://schema-manager-server.internal.test.adtechplatform.sky.com/api"
    }
