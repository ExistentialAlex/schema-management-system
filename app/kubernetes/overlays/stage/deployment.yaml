---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: <platform-team-name>-app
spec:
  template:
    metadata:
      annotations:
    spec:
      containers:
        - name: adtech-adf-app
          image: europe-west1-docker.pkg.dev/uk-cti-adtech-platform-stage/<platform-team-name>/<platform-team-name>-app:${GIT_TAG}
