---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: schema-manager-app
spec:
  template:
    metadata:
      annotations:
    spec:
      containers:
        - name: adtech-adf-app
          image: europe-west1-docker.pkg.dev/uk-cti-adtech-platform-stage/schema-manager/schema-manager-app:${GIT_TAG}
