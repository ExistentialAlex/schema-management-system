version: 2
updates:
  - package-ecosystem: 'npm' # See documentation for possible values
    directory: '/'
    versioning-strategy: 'increase'
    schedule:
      interval: 'daily'
    cooldown:
      default-days: 14 ## Minimum age of package updates before PRs are raised
      include:
        - '*'
